module.exports = {
  config: {
    name: "vcppr",
    version: "1.0",
    author: "ashik",
    role: 0,
    description: "Help menu"
  },

  onStart: async function ({ api, event }) {
    return api.sendMessage(
`╭── VCPP ESPORTS ──╮

Reply photo + use:
/vcpp color ITS NAME , DOWN , GC

Example:
/vcpp yellow ITS XAIKO , SIYAM , KO KING TOP VOICE G

╰────────────────╯`,
      event.threadID,
      event.messageID
    );
  }
};
